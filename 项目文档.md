# electrochem-platform 项目文档

> **项目名称**：树莓派在电化学设备的应用开发 —— 溶液导电性相对比较实验平台
> **文档性质**：基于当前最新代码（main @ `abf7448`，2026-09-05 复核更新；原稿基线为 09-03 工作区快照）编写的全面项目文档
> **文档范围**：架构、模块、接口、数据、配置、测试、部署、已知问题与 Bug 清单
> **配套文档**：根 `README.md`（快速开始与进度真相）、`docs/`（接口/数据格式/交接/评审记录）

---

## 1. 项目概述

### 1.1 定位

本项目是一个面向「溶液导电性相对比较实验」的**端到端测量演示平台**：

- 对导电池施加受控交流激励，采集电极两端**电压**与回路**电流**并同步读取**温度**；
- 软件链路完成 `G(T)=I/U → κ(T)=Kcell·G(T) → κ25`（温度补偿）的计算与展示；
- 提供实时 Web UI（波形/散点/统计/比较）、历史数据存储（SQLite，append-only）与 CSV/JSON 导出、化学公式拟合（线性/二次/一阶饱和/Arrhenius/Kohlrausch）与自动判稳 QC。

当前硬件未接入，**全部数据由模拟数据源（Mock / 电压扫描 Simulator / CSV 回放）产生**；但驱动层已按统一异步边界抽象，真实 ADC（候选 ADS1256）接入时只替换驱动、不动业务层。

### 1.2 关键设计决策（历史路线变更）

| 决策 | 说明 |
|---|---|
| 放弃“电导率模块直出 EC” | 原 BA121S/CM2 现成电导率模块路线已废弃（见 `docs/电导率I-V测量链路与开发路线.md`），改走 **U/I 采集 → 电导 → Kcell → 温补** 路线 |
| 数据分层 | Raw（`voltage_raw_v/current_raw_a/temperature_raw_c`，不可变）→ Calibrated（`conductance_s/kappa_t_us_cm`）→ Derived（`kappa_25_us_cm/QC/拟合`），配 Trace/Configuration/Quality 分层 |
| 原始帧不可篡改 | SQLite 触发器禁止 `raw_frames` 的 UPDATE/DELETE |
| 单一采集任务 | 无论多少浏览器连接，只由**一个**后台任务采集/落库/广播，避免多连接重复采集、无连接漏采 |
| 模拟与真实解耦 | 前端切地址（`VITE_WS_URL/VITE_API_BASE`）即可切真实后端；前端还有纯浏览器模拟模式（`VITE_DATA_SOURCE=browser`） |

### 1.3 进度状态

- ✅ **Phase 0.x**：环境/骨架/健康接口/仓库初始化
- ✅ **Phase 3（M1–M2）**：Web 前端 + 模拟数据源闭环（F01–F10）
- ✅ **Phase 2 集成加固**：统一驱动接口、可配置 Mock 场景、慢客户端隔离、SQLite 长跑
- ✅ **Phase 7（M3–M4 前置）**：SQLite 存储 + 历史查询 + 导出
- ✅ **备选公式拟合**：按 X 轴语义拟合 + 统计量（M4 前置）
- ⏳ **后续**：真实电压/电流/温度采集（DS18B20、AFE/ADC 台架）、Kcell 标准液校准、温补验证、PDF 报表、ESP32-S3 实时控制

> 冻结 SRS 中若干强制项（REQ-M-001 真实采集、REQ-F 报表、REQ-D-003 阈值标定、REQ-C-001 标准液 SOP、REQ-U-001 滤波分层）仍未全部满足，详见根 `README.md`「已知缺口与交付阶段」，勿将当前交付误判为完整 REQ 满足。

---

## 2. 仓库结构

```
electrochem-platform/
├── backend/            # FastAPI 后端（Python ≥3.11，实测 3.12/3.13）
│   ├── app/
│   │   ├── main.py             # 应用入口：lifespan、CORS、托管 frontend/dist
│   │   ├── routes.py           # WS 实时流 + REST 控制 + 历史/导出 + 拟合 + 调试接口
│   │   ├── schemas.py          # Pydantic 模型（帧/控制/拟合协议）
│   │   ├── state.py            # 实验内存态单例（running/stopped/idle + 溯源上下文）
│   │   ├── measurement.py      # I–V 计算链纯函数 G→κ(T)→κ25
│   │   ├── analysis.py         # 备选公式拟合（纯 Python，无 numpy）
│   │   ├── stability.py        # 自动判稳/QC 纯函数
│   │   ├── storage.py          # SQLite DAO + 版本化迁移 + append-only 触发器
│   │   ├── persistence.py      # 后台异步批量落库服务（攒批 ≤200 条 / 0.5s）
│   │   ├── broadcast.py        # 每客户端有界队列的 WS 广播枢纽
│   │   ├── stream.py           # 简单 EC 模拟帧生成（调试 burst 用）
│   │   └── drivers/            # 设备驱动统一异步边界
│   │       ├── base.py              # DeviceDriver ABC + DriverReading
│   │       ├── mock.py              # 确定性可配置 Mock（默认）
│   │       ├── simulator.py         # 电压扫描仿真器（stable/realistic/fault）
│   │       └── csv_playback.py      # echemdb CSV 回放
│   ├── tests/              # pytest 单测/协议测试
│   └── requirements*.txt   # 运行/开发依赖（*.lock.txt 为锁定版本）
├── frontend/           # React 18 + TS + Vite + ECharts
│   └── src/
│       ├── config/config.ts      # 配置唯一入口
│       ├── types/protocol.ts     # 协议类型 + WS 消息解析校验
│       ├── services/             # WS 客户端 / REST 客户端 / 浏览器模拟 / Bridge
│       ├── lib/                  # 纯函数：I–V 分析、单位换算
│       ├── hooks/                # useConnection/useExperiment/useRealtimeData/useIVAnalysis
│       ├── components/           # 图表与 UI 组件
│       └── pages/ExperimentPage.tsx
├── configs/            # 版本化设备/校准配置 JSON（schema_version）
│   └── devices/        # mock.example.json / simulator.{example,fault,realistic}.json
├── data/               # raw(不入 Git)/calibrated/derived + fixtures/ingest 测试数据
├── firmware/           # ESP32-S3 固件（占位）
├── tests/e2e/          # Playwright 验收（F/P 用例 + 截图 + P03 长跑）
├── scripts/            # deploy(systemd+kiosk)、run_backend、run_simulator、soak/ingest
├── docs/               # 接口说明/接入数据格式/交接/评审记录/已知问题/截图
└── .github/workflows/ci.yml
```

---

## 3. 技术栈与运行环境

| 端 | 技术 | 版本约束 |
|---|---|---|
| 后端 | Python + FastAPI + uvicorn + pydantic v2 | Python ≥3.11（`.nvmrc`=Node 24；CI 用 Python 3.12，目标机 Debian trixie 为 3.13） |
| 数据库 | SQLite（WAL），自研版本化迁移 | 锁文件 `requirements.lock.txt`：fastapi==0.141.1 / uvicorn==0.52.4 / pydantic==2.13.4 |
| 前端 | React 18 + TypeScript ~5.6 + Vite 8 + Apache ECharts 6 + CSS Modules + 原生 WebSocket | Node ≥24；构建命令含 `tsc --noEmit` |
| E2E | Playwright（chromium 或 `E2E_BROWSER=msedge/chrome` 系统浏览器） | Node ≥24；`STABILITY_MINUTES` 控制 P03 长跑 |
| 目标机 | Raspberry Pi OS（桌面版），systemd + Chromium Kiosk | 生产由 FastAPI `:8000` 同源托管前端产物 |

---

## 4. 系统架构与数据流

### 4.1 总体分层

```
┌──────────────────────── 前端（React/Vite/ECharts）────────────────────────┐
│  config → bridge(server|browser) → hooks（连接/实验/实时缓冲/IV分析）→ 组件   │
│   实时流: WS /ws/stream        控制+历史+拟合: REST /api/...                 │
└───────────────┬────────────────────────────────────────┬───────────────────┘
                │                                        │
┌───────────────▼────────────────────────────────────────▼───────────────────┐
│  后端 FastAPI（:8000）                                                        │
│  ┌──────────────────────────────────────────────────────────────────┐       │
│  │ 单一采集任务（acquisition_loop）: running 时 10Hz 读取 driver      │       │
│  │   → _build_frame（U/I/T → 计算链 G→κ(T)→κ25）                     │       │
│  │   → persist.enqueue_frame（异步落库，append-only）                 │       │
│  │   → _hub.publish（每客户端有界队列广播）                           │       │
│  └──────────────────────────────────────────────────────────────────┘       │
│  drivers/ : MockDevice（默认）| SimulatorDriver | CsvPlaybackDriver  →(未来) ADS1256│
│  storage/ : experiments / samples / raw_frames / calibration_records /        │
│             fit_results（schema_migrations 驱动 v1→v6 迁移）                  │
└───────────────────────────────────────────────────────────────────────────┘
```

### 4.2 测量计算链（backend/app/measurement.py）

```
G(T)      = I / U                                   [S]
κ(T)      = Kcell × G(T) × 1e6                      [μS/cm]
κ25       = κ(T) / [1 + α×(T−25)]                   [μS/cm]
```

- 校验：U 必须 > 0（否则电导无定义）；Kcell > 0；温补分母不得为 0。
- U ≤ 0 / 非有限值的帧（如 CV 数据电极电位为负）：原始 U/I/T **仍落库**（Raw 不可变），Derived 置 null，`quality_flags` 追加 `COMPUTE_INVALID`，业务不崩溃。
- Mock/Simulator 的“目标 κ25 → 反推 I → 计算链还原 κ25”保证软件链路闭环可验。

### 4.3 数据分帧字段（帧溯源，rule 38）

每帧携带 `seq_no / timestamp_utc / monotonic_ms / sensor_path_id` 溯源字段，原始列与计算列分开存储（见 §8）。

---

## 5. 后端设计详解

### 5.1 模块职责

| 模块 | 职责 | 关键点 |
|---|---|---|
| `main.py` | 应用装配 | lifespan 内 `state.reset() → persist.start() → start_acquisition()`；关闭反向停止并在 running 时 flush + finish(aborted)；CORS 允许 localhost/10.x/192.168/172.16-31/*.local；若存在 `frontend/dist` 则挂载 `/` 静态托管（须在 API 路由之后） |
| `state.py` | 实验内存态 | `idle/running/stopped`；`start()` 原子绑定新实验上下文；`resume()` 扣除暂停墙钟续跑；`next_seq()` 线程安全自增 |
| `routes.py` | 全部 HTTP/WS 入口 | 见 §5.2 |
| `measurement.py` | 计算链 | 纯函数 |
| `analysis.py` | 拟合 | 纯 Python 高斯消元解正规方程；非线性（一阶饱和）用 k 对数网格+线性 LS；Arrhenius/Kohlrausch 线性化；输出 R²/RMSE/MAE/AICc/残差峰/Wald CI/LOOCV/有效区间 |
| `stability.py` | 判稳/QC | 窗口=最近 30 点，min_samples=10；先查硬质量标志（SATURATED/OPEN_CIRCUIT/…→FAIL），再判 drift（斜率）与 CV（变异系数）；PASS/WARN/FAIL |
| `storage.py` | SQLite DAO + 迁移 | `SCHEMA_VERSION=6`；迁移 v2–v6 每步独立事务、禁 executescript；v4 重建表解 `ec_raw NOT NULL`；append-only 触发器 |
| `persistence.py` | 异步落库 | `asyncio.Queue` + 单写入任务；攒批 ≤200 条或 0.5s；flush 屏障保证“调用前已入队的帧均已提交”；失败置 `degraded` 并拒收后续帧 |
| `broadcast.py` | WS 广播 | 每连接一个发送任务 + 有界队列（20 000）；队列满丢最旧保最新，慢客户端不阻塞采集 |

### 5.2 REST / WS 接口总览

**控制与实时流**

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/health` | `{status, experiment, persistence, persistence_error}` |
| WS | `/ws/stream` | 连接即订阅广播；数据帧含 `timestamp/ec/temperature/status` 与 I–V 扩展字段；状态帧只有 `status`（+message/persistence） |
| POST | `/api/experiment/start` | 可选 body：`sample_id/sensor_path_id/title/operator/objective/concentration_mmol_l(≥0)`。运行中重复 start → `ok:false`。**停止后同样品 start = 续跑同实验**（`resumed:true`，不新建记录，仅把 DB 记录的 status 改回 `running` 并清空 `ended_at_utc`）；换样品/先 reset 则开新实验 |
| POST | `/api/experiment/stop` | running→stopped；flush → 写 QC（`_compute_and_store_qc`，对最近 500 帧判稳写回 samples）→ `finish_experiment(stopped)`；重复 stop 不刷新结束时间 |
| POST | `/api/experiment/reset` | 清内存态为 idle；若 running 中被打断则落库标记 `aborted` |
| GET | `/api/experiment/current` | 重连/刷新后恢复 `experiment_id/sample_id/experiment_uid` |

**历史与导出（Phase 7）**

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/experiments` | 列表（含 frame_count，id 倒序） |
| GET | `/api/experiments/{id}` | 详情：元信息 + `samples[]` + frame_count + `calibrations[]` + `fits[]` |
| GET | `/api/experiments/{id}/frames?limit&offset` | 帧分页（limit ≤100 000） |
| GET | `/api/experiments/{id}/export.csv` | 全部帧 CSV（attachment） |
| GET | `/api/experiments/{id}/export.json` | 完整实验 JSON（含帧，超 100 万帧截断并置 `truncated`） |

**拟合（M4 前置）**

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/analysis/fit` | body `{x[], y[], models?, x_axis: time\|temperature\|concentration, experiment_id?}`；按 R² 降序返回；带 experiment_id 时写入 `fit_results` 并输出 `derived_path`（`data/derived/experiment_{id}_fit_{axis}.json`） |

**调试（仅模拟源，需 `EC_ENABLE_DEBUG_ENDPOINTS` 显式开启）**

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/debug/bad-frame` | 注入 `ec:"abc"` 非法帧（F10 容错） |
| POST | `/api/debug/close-connections` | 强制断开所有 WS（F08/F09） |
| POST | `/api/debug/burst?count=` | 快速推 ≤10 000 帧（P03/P04），仅广播不落库 |

### 5.3 设备驱动（rule 34 统一边界）

| 驱动 | 选择方式 | 特点 |
|---|---|---|
| `MockDevice` | `EC_DRIVER=mock`（缺省） | 确定性（seed）；scenario：stable/noisy/drift/dropout；按 κ25 目标反推 U/I/T，`calibration_claimed=true` |
| `SimulatorDriver` | `EC_DRIVER=simulator` | 电压扫描（0.2→1.0 V）+ 极化一阶滤波 + mode 预设 stable/realistic/fault（fault_kind：dropout/current_zero/voltage_oor/adc_sat/temp_abnormal/unstable/disconnected）；`calibration_claimed=false`（标注 simulated） |
| `CsvPlaybackDriver` | `EC_DRIVER=csv` + `EC_CSV_PATH` | 回放 echemdb 4 列（time_s/voltage_v/current/temperature_c）；可调速/循环；电压≤0 → COMPUTE_INVALID |

驱动选择在 `routes._build_driver()`，未来 ADS1256 适配器在此注册，业务层不分支。

### 5.4 落库与降级语义

- 实验/样品在同一事务创建（`create_experiment_with_sample`），失败不残留空记录。
- 每帧经 `_enqueue_frame` → 攒批 INSERT（含样品 frame_count 累计）。
- **落库失败**：持久化置 `degraded`（进程级闩锁），队列拒收；WS 仅**一次**推送 `PERSIST_DEGRADED_MESSAGE`；帧追加 `PERSIST_DROPPED` 质量标志后仍广播（实时曲线不中断，历史和导出缺帧）。
- stop/reset 时 `flush()` 屏障确保帧已提交再 `finish_experiment`。

---

## 6. 前端设计详解

### 6.1 数据通路

```
WebSocket / BrowserMock
   └─(事件总线 ClientEvent: message|status|connection|error)
        ├─ useConnection   : 连接状态 + 错误横幅 + 手动重连
        ├─ useExperiment   : 实验状态机 + 控制动作（start/stop/reset/restart）+ experimentId
        └─ useRealtimeData : pointsRef（可变数组，上限 20 000 点，超出丢最旧）+ count/latest
              └─ useIVAnalysis(useMemo) : O(n) I–V 摘要
                    └─ 组件渲染：WaveformChart / IVChart / SolutionCompare / ResultPanel / …
```

- **消息校验**（`types/protocol.ts#parseServerMessage`）：非法 JSON、缺 timestamp/temperature、非数字 ec、未知 status → 返回 null 丢弃并提示（F10）；`ec:null`（COMPUTE_INVALID）合法。
- **断线恢复**：close → `disconnected` + 提示 + 指数退避重连（1s→5s）；连接成功且 server 模式时拉 `/api/experiment/current` 恢复 experiment_id/样品（避免导出按钮消失）；看门狗仅在 running 且 3s 无数据时提示“数据流超时”。
- **续跑（resumed）**：`handleStart` 检测 `resumed` 后拉取该实验历史帧 `hydrateFromFrames` 灌入缓冲，再续接实时帧；换实验则 `clearPoints`。
- **性能**：数据放 ref 避免 10Hz 全量 re-render；曲线组件 200ms 节流读 ref 更新；大点数降采样（lttb / strideSample）；ECharts `animation:false`。

### 6.2 页面/组件地图

| 区域 | 组件 | 内容 |
|---|---|---|
| 顶栏 | ExperimentInfo / StatusBadge / HistoryPanel 入口 | 标题、状态徽标、历史实验面板 |
| 控制区 | ControlBar + ConnectionPanel + 样品/浓度输入 | 开始/停止/重新开始/清空；溶液编号与浓度（mmol/L，≥0） |
| 数值区 | ValueDisplay ×4 + DataStats | 电极温度 / 电压 / 电流（自动 μA↔mA）/ κ25；点数/时长/采样率/激励 |
| 实时图 | WaveformChart（V(t)/I(t) 双轴） | 交流激励波形，用于看噪声/稳定 |
| 核心图 | IVChart + ExperimentResultCard | I–V 散点 + OLS 拟合线（仅线性成立时）；G/R/κ25/R²/温度结果卡 |
| 溶液比较 | SolutionCompare | 本页会话内累计的 κ 柱状图+表（仅当前会话，不入历史库） |
| 结果区 | ResultPanel + FitPanel | QC 徽标/代表值/中位数/原因；基础统计；化学公式拟合（X 轴 time/temperature/concentration，模型池按轴隔离） |
| 诊断 | DiagnosticsPanel | 折叠 EC-t 实时曲线（展开才挂载）+ G/κ(T) 值 |
| 历史 | HistoryPanel + StaticChart | 列表→详情（样品表、静态曲线降采样 2000 点、CSV/JSON 导出、复用 FitPanel） |

### 6.3 I–V 分析逻辑（lib/ivAnalysis.ts）

- 有效点需 `voltage_raw_v` 与 `current_raw_a` 均为有限数；`G` 优先取后端 `conductance_s`，否则取 `I/U`（U>0）。
- 线性判定门槛：点数 ≥3、电压跨度 ≥0.05 V、R² ≥0.95 → `linearOk`，`G=OLS 斜率`；否则 `G=各点 I/U 均值`、`R=1/G`、`κ25=均值`，并给出中文原因（voltage_span_too_small / nonlinear / …）。

---

## 7. 前后端协议要点

1. WS 数据帧（v2 扩展后）字段：`timestamp, ec(nullable), temperature, status` + `schema_version, device_id, firmware_version, range_id, voltage_raw_v, current_raw_a, temperature_raw_c, conductance_s, kappa_t_us_cm, kappa_25_us_cm, quality_flags, calibration_id, excitation_frequency_hz, excitation_amplitude_v, compensation_model`。
2. **`ec` 是 κ25 的兼容别名**；COMPUTE_INVALID 时 `ec`/κ25 为 null，但原始 U/I/T 仍下发。
3. `quality_flags` 为 `|` 分隔字符串；模拟源至少带 `SIMULATED`；未校准补 `UNCALIBRATED`；计算链拒绝补 `COMPUTE_INVALID`；落库拒收补 `PERSIST_DROPPED`。
4. CSV/JSON 导出的规范列名：`ec_raw_us_cm/temperature_raw_c/k25_us_cm/kappa_25_us_cm/…`（详见 `docs/接口说明.md`、`docs/接入数据格式.md`）。

---

## 8. 数据模型与存储

**数据库**：`data/raw/ec.db`（可用 `EC_DB_PATH` 覆盖，不入 Git）。WAL + foreign_keys ON。**Schema 版本 = 6**，`schema_migrations` 记录已应用版本。

| 表 | 关键列 | 备注 |
|---|---|---|
| `experiments` | experiment_id(UNIQUE), title, operator, objective, started_at_utc, ended_at_utc, status, sample_id, sensor_path_id, metadata_json | status：running→stopped/aborted/error |
| `samples` | UNIQUE(experiment_id, sample_id, sensor_path_id), concentration_mmol_l, k25_median/mean/sd, frame_count, qc_status/reason/representative_value/qc_checked_at_utc | 停止时写 QC |
| `raw_frames` | 见下 | **append-only**（触发器禁 UPDATE/DELETE） |
| `calibration_records` | calibration_id, mode, standard, lot, coeff_value, coeff_json | 每次 start 写入 mock 校准（REQ-C-001 软件侧） |
| `fit_results` | x_axis, model, params_json, r2/rmse/mae/aicc/loocv_rmse, x_min/x_max, extrapolation_forbidden, param_ci_json, derived_path | 每 (experiment_id, x_axis) 覆盖写 |

`raw_frames` 核心列：`experiment_id, sample_id, sensor_path_id, seq_no, timestamp_utc, monotonic_ms, t_seconds, ec_raw, temperature_raw, k25, quality_flags, status, inserted_at_utc` + v2 计算列 `voltage_raw_v, current_raw_a, conductance_s, kappa_t_us_cm, kappa_25_us_cm` + v5 溯源列 `schema_version, device_id, firmware_version, range_id, calibration_id, excitation_frequency_hz, excitation_amplitude_v, compensation_model`。

**迁移历史**（每步独立事务，`tests/test_migrations.py` 覆盖旧库升级）：
v1 基线 → v2 加 I–V 列 → v3 samples 加 QC 列 → v4 重建表放开 `ec_raw NOT NULL` → v5 加溯源/激励列 → v6 建 `fit_results`。

---

## 9. 配置体系

### 9.1 设备配置（configs/devices/，schema_version="1.0.0"）

| 文件 | 用途 |
|---|---|
| `mock.example.json` | Mock 场景（stable、seed、噪声、dropout_every_n…） |
| `simulator.example.json` | 电压扫描仿真（stable 预设） |
| `simulator.realistic.json` | realistic 预设（含漂移/非线性/极化） |
| `simulator.fault.json` | fault 预设（fault_kind=dropout, dropout_every_n=8, fault_start_s=0.3） |

JSON 顶层可带 `driver / schema_version / simulated`；未知字段会被严格拒绝（白名单/字段集校验）。

### 9.2 环境变量总表

**后端（EC_*）**

| 变量 | 默认 | 用途 | 文档化 |
|---|---|---|---|
| `EC_DRIVER` | mock | mock/simulator/csv 三选一 | ✅ |
| `EC_MOCK_CONFIG / EC_MOCK_SCENARIO / EC_MOCK_SEED` | 内置默认 | Mock 配置覆盖 | ✅ |
| `EC_SIM_CONFIG / EC_SIM_MODE / EC_SIM_G` | — | 仿真器配置/模式/目标 G | ✅ |
| `EC_SAMPLE_RATE_HZ` | 10 | mock/simulator 共享采样率 | ✅ |
| `EC_CSV_PATH / EC_CSV_SPEED / EC_CELL_CONSTANT` | — | CSV 回放 | ⚠️ 部分散见 |
| `EC_DB_PATH` | data/raw/ec.db | 数据库路径 | ✅ |
| `EC_ENABLE_DEBUG_ENDPOINTS` | off | 调试接口开关 | ✅ |
| `EC_CALIBRATION_ID` | 驱动自带 | start 时覆盖校准 id | ⚠️ 仅评审记录提及 |
| `EC_CORS_ORIGINS / EC_CORS_ORIGIN_REGEX` | 局域网正则 | CORS | ✅ |
| `EC_FRONTEND_DIST` | frontend/dist | 生产托管目录 | ❌ 未文档化 |
| `EC_DERIVED_DIR` | data/derived | 拟合 JSON 报告目录 | ❌ 未文档化 |
| `EC_CSV_SAMPLE_RATE_HZ / EC_SIM_SEED` | — | 见代码 | ❌ 未文档化 |
| `EC_BIND / EC_PORT` | 127.0.0.1 / 8000 | uvicorn 监听（setup.sh 注入 BIND） | ⚠️ EC_PORT 未文档化 |

**前端（VITE_*，.env.local）**

| 变量 | 默认 | 用途 |
|---|---|---|
| `VITE_DATA_SOURCE` | server | server=连后端；browser=纯浏览器模拟 |
| `VITE_WS_URL` | 当前主机:8000/ws/stream | WS 地址 |
| `VITE_API_BASE` | 当前主机:8000 | REST 地址 |

---

## 10. 测试体系

### 10.1 后端 pytest（backend/tests/，需装 requirements-dev）

覆盖：协议解析/API（start/stop/reset/current/health）、状态机、计算链、判稳/QC、存储与迁移（旧库升级 v1→v6）、broadcast 慢客户端、CSV 回放、simulator 各 fault、mock 场景、公开数据 ingest、评审回归用例（test_review_fixes / test_verified_review_fixes）。

> 注：2026-09-03 执行结果以文末「附录 A」实测为准。

### 10.2 前端单测

`npm test`（Node 内置 test runner + `--experimental-strip-types`）：`protocol.test.ts`（WS 消息解析容错）、`ivAnalysis.test.ts`、`units.test.ts`、`controlError.test.ts`（控制响应横幅判定）—— **实测 18/18 通过（2026-09-05）**。`npm run typecheck`（tsc --noEmit）通过。

### 10.3 E2E 验收（tests/e2e/，Playwright）

| 维度 | 用例 |
|---|---|
| F01–F10 功能验收 | 启动/禁用态/数值与单位/曲线追加/波形轴/WS 连接/断线提示/自动重连/坏帧不崩/停止冻结 |
| P 系列 | P02 10Hz 持续流、P04 1 万点不卡、P03 30 分钟长跑（`STABILITY_MINUTES` 控制，默认 skip，不进 CI） |
| 拟合 | 时间轴拟合、切换模型竞态、Arrhenius 温度跨度跳过、浓度轴模型池、历史详情复用拟合 |
| Phase 7 | 历史列表→详情→样品溯源→CSV 导出（HTTP 断言，规避 Edge 的 `<a download>` 事件问题） |
| 截图 | 三状态 + 历史面板截图 → `docs/screenshots/`（CI 忽略） |

浏览器选择：默认 Playwright chromium；设 `E2E_BROWSER=msedge/chrome` 走系统浏览器。webServer 自动拉起 uvicorn(:8000, `EC_ENABLE_DEBUG_ENDPOINTS=1`, 临时 DB) 与 `vite dev`(:5173)。

### 10.4 CI（.github/workflows/ci.yml）

- push→main 与全部 PR 触发。
- Job1 backend：Python 3.12 + `pytest tests -q`。
- Job2 frontend：Node 24 + `npm ci` + typecheck + 单测。
- Job3 e2e：Node 24 + Python 3.12 + `npx playwright install --with-deps chromium` + 全量 spec（screenshots/stability 由 config 忽略）。
- 不足：无覆盖率门槛；backend/e2e 用松版本依赖（`>=`），与部署锁文件（requirements.lock.txt）存在漂移风险；CI Python 3.12 与部署目标 3.13 未对齐；未上传 Playwright trace/HTML 报告。

---

## 11. 部署

### 11.1 开发环境

```bash
# 后端 :8000
cd backend && python -m venv .venv && .venv/Scripts/pip install -r requirements-dev.txt
.venv/Scripts/python -m uvicorn app.main:app --host 0.0.0.0 --port 8000
# 前端 :5173
cd frontend && npm install && npm run dev
```

### 11.2 树莓派生产（scripts/deploy/setup.sh）

1. 校验非 root、`EC_BIND`（默认 127.0.0.1，局域网用 `EC_BIND=0.0.0.0`）。
2. 前端：无 node_modules 才 `npm ci`；`npm run build` 产出 `frontend/dist`。
3. 后端：建 `backend/.venv`，优先装 `requirements.lock.txt`（不装 pytest/httpx）。
4. systemd：唯一服务 `ec-backend`（`User/WorkingDirectory/Environment=EC_BIND/ExecStart=run_backend.sh`）；自动停用删除旧 `ec-web.service`（:5173 已废弃）。
5. Kiosk：检测 chromium → raspi-config `do_boot_behaviour B4`（桌面自启）；Labwc 分支写 autostart，X11/LXDE 分支写 `~/.config/autostart/ec-kiosk.desktop`；URL 固定 `http://localhost:8000`。

> 旧文档（docs/项目交接文档.md §12、根 REVIEW.md）中「双 systemd 服务 / :5173 http.server 托管」的描述已被上述单服务方案取代，属**过期文档**。

---

## 12. 已知问题与 Bug 清单（reviewer 视角）

> 来源：2026-09-03 全量代码审读 + 前端组件/部署配置专项审查 + 既有 `docs/已知问题.md`、V2 事故复盘（注：早期评审稿 REVIEW_round4/5 未入库，其结论已转录至本文，引用文件本身不存在）。
> **2026-09-05 复核**：基于 main @ `abf7448` 逐项核验本清单——已修复项标 ✅ 并附提交/PR 号，新增 N1–N4（本轮复核新发现）。
> 分级：🔴高（影响正确性/数据/性能）、🟠中、🟡低。

### 12.1 代码级 Bug（本轮新发现/复核）

| # | 级别 | 位置 | 问题 | 后果 | 建议 |
|---|---|---|---|---|---|
| B1 | 🔴 | `frontend/src/components/WaveformChart.tsx` ~139-155 | 电流自动 μA↔mA 换挡后 Y 轴边界“只扩不缩”且跨单位混算（旧 μA 值 vs 新 mA 值） | 右轴被钉在旧量程，波形压成直线/越界，长期不可读 | 检测 `iUnit` 切换时重置 `iBounds`，或每次从数据重算 |
| B2 | 🔴 | `RealTimeChart.tsx` 160-176 + `useRealtimeData.ts` 57-59 | 缓冲到 20 000 上限后 `arr.splice` 原地裁剪，增量失效退化为每 200ms 全量 2 万点 `setOption` | 长跑（>2 万帧）CPU/GC 压力骤增，RPi 端卡顿 | 检测首点位移做滚动窗口或全量重建按需触发 |
| B3 | 🟠 | `RealTimeChart.tsx` 118-124 / `WaveformChart.tsx` 146-154 | Y 轴范围同实验内只单调扩展（splice 不换对象 → 不 reset） | 早段尖峰把轴永久顶大，稳态信号被压平 | 引入“离开轴范围长时间则重算/衰减”逻辑 |
| B4 | 🟠 | `IVChart.tsx` 102-122 | effect 依赖引用型 `analysis.fitLine`（10Hz 重建）→ 高频清理/重建 interval | 冗余 setOption 与 interval 抖动 | 运行态只建一次 interval，analysis 走 ref |
| B5 | 🟠 | `IVChart.tsx` 105-117 | 电流换挡瞬间散点量纲与 Y 轴名短暂不一致 | 阈值附近曲线抖动 | 单位决策与数据同帧切换并加迟滞 |
| B6 | 🟠 | `RealTimeChart.tsx` 171-187 | `renderedCount` 用未过滤点数计（含 null-ec 点），与实际 append 数据数不一致 | 无效点累积导致提前触发全量重绘 | 计数与实际 data 长度同口径 |
| B7 | 🟡 | `StaticChart.tsx` 51-96 | `notMerge:true` 全量重建依赖引用型 props；Y 轴名硬编码 EC (μS/cm) | 父级高频渲染时白费计算；非 EC 叠加线误导 | 上层 useMemo 稳定数组；Y 轴名可覆盖 |
| B8 | 🟡 | `ValueDisplay.tsx` ~18 | 仅判 null，不判 NaN/±Infinity | 脏数据可能显示 “NaN” | 统一 `!Number.isFinite` 守卫 |
| B9 | 🟡 | `ivAnalysis.ts` 193-208 | 电流恒定（ssTot≈0）时 R² 硬编码 1 → `linearOk=true`、斜率≈0 的“完美拟合” | 若电压跨度达标会把 G≈0 当结论 | 增加斜率/电流离散度最小阈值并补单测 |
| B10 | ✅ 已修 | `routes.py` resume 分支（~423） | 续跑成功路径未调用 `_reset_persist_notice()`（新实验路径调用） | 降级通知状态在不同路径不一致 | ✅ 已修复（PR #18，`4f0dc81`）：resume 分支现调用 `_reset_persist_notice()` |
| B11 | ✅ 已解决 | `routes.py` stop（~512） | 非 running 时调用 stop 返回 `ok:true` + 提示文案 | 语义上“成功”与“无事发生”混用 | ✅ 已按设计解决（PR #20）：幂等 stop 不再带 message；前端新增 `lib/controlError.ts`，仅 `!ok` 或 `persistence=degraded` 才弹错误横幅 |
| B12 | ✅ 已修 | 前端 `useRealtimeData.hydrateFromFrames`（~26-35） | 续跑时 `history 帧(≤t0) + 实时缓冲(>t0)` 拼接，未按 t 去重，首尾可能重复一帧 | 评审 P1-5 标“❌ 仍存在” | ✅ 已修复（`01940e5`，08-24 合入，早于本文 09-03 基线——即原稿记录时已过时）：拼接前按 `p.t > lastT` 过滤 |
| B13 | 🟡 | `main.py` lifespan | 模块级单例 + 测试/重载可能重复进 lifespan，依赖 state.reset 兜底 | 边界条件多 | 已有注释说明，保持纪律 |

### 12.1.1 2026-09-05 复核新增（N 系列）

| # | 级别 | 位置 | 问题 | 后果 | 建议 |
|---|---|---|---|---|---|
| N1 | 🟠 | `backend/app/analysis.py:150` `_pack` | `r2 = 1.0 if ss_tot < 1e-12`：y 恒定时所有模型 R² 并列 1.0 | 退化数据被当“完美拟合”，误导模型选型并写入 fit_results（与前端 B9 同型，后端侧此前漏记） | y 恒定时 r2 置 None 或标记模型无效，并补单测 |
| N2 | 🟡 | `backend/app/state.py:82-89` `reset()` | 不还原 `sample_id/sensor_path_id` 默认值 | 仅 reset 后经 `/api/experiment/current` 可见上一实验的样品号（start 会覆盖，影响窗口很小） | reset 时一并还原 |
| N3 | 🟡 | `backend/app/stability.py:52-66` | `_Window` 类为死代码（`check_stability` 直接列表切片，未用到该类） | 阅读困惑、维护负担 | 删除或改为实际使用 |
| N4 | 🟡 | `backend/app/{api,db,domain,services,runtime,export,adapters}/` | 7 个目录仅含 `__pycache__`（源码已删的包化重构残留，未入 Git，不影响运行） | 极强误导性——目录结构会让人误以为后端已包化（实际为平铺模块） | 手动删除这 7 个残留目录 |

### 12.2 架构/工程级遗留（跨 REVIEW 轮次未收敛）

1. ✅ **已修复（PR #18/#19，09-05 复核）**：晚连/刷新无感知（P1-A）→ `ws_stream` 连接时若 `persist.degraded` 补发告警（`4f0dc81`）；启动兜底清理（P1-B）→ `persistence` 启动时 `abort_stale_running_experiments()` 把遗留 running 行标 aborted（`a465ae2`）。**残留**：`degraded` 仍是进程级永久闩锁，DB 恢复后不自愈（需重启进程）。
2. ✅ **已修复（PR #20，09-05 复核）**：前端 `useExperiment.run()` 把任意 `message` 当错误横幅 → 新增 `lib/controlError.ts`，仅 `!ok` 或 `persistence=degraded` 弹横幅，幂等提示（如“实验已在进行中”）不再当错误；降级通知重发由 P1-A 的连接补发覆盖。
3. **P2-C（未修，09-05 复核确认）**：`EC_CALIBRATION_ID` 环境变量覆盖 `cal_id`（routes.py:442-443）但不翻转 `calibration_claimed`（routes.py:220 据此打 `UNCALIBRATED`）→ 帧可同时带 `UNCALIBRATED` 标志与非空 `calibration_id`，溯源自相矛盾。
4. **P2-D（09-05 复核降级为理论问题）**：`routes.py:438` 的 `body.sensor_path_id or DEFAULT_SENSOR_PATH_ID` 已把 None/空串归一为默认值，“空串 vs NULL 分裂”经 REST 正常路径不可达；仅绕过 routes 直调 `insert_frames` 才可能触发。
5. **Kiosk 无就绪轮询**：`setup.sh` 的 `sleep 5` 硬编码，冷启动 ec-backend 未就绪时 Chromium 打开失败页且不重试。
6. **图表性能债**：WaveformChart 等 200ms 全量 `setOption`（20k 点×序列）在 RPi 上是已知债务（评审接受项）。

### 12.3 文档与代码不一致（建议修正）

| # | 问题 |
|---|---|
| D1 | `docs/项目交接文档.md`：双 systemd 服务（ec-web :5173）、pytest “36 项”、E2E “20 用例”等已过期（现状：单 ec-backend、pytest 131、E2E 26 用例）；仓库/Node 路径为旧机器 |
| D2 | `frontend/README.md` 写 “Vite 5”，package.json 实际 `vite ^8.2.2` |
| D3 | `schema_version` 三义混用：实时帧恒=2（routes.py）、DB SCHEMA_VERSION=6（storage.py）、设备 JSON="1.0.0"（string） |
| D4 | `docs/接入数据格式.md` 迁移描述停留在 v5，未更新到 v6（fit_results） |
| D5 | 若干环境变量未文档化：`EC_PORT / EC_FRONTEND_DIST / EC_DERIVED_DIR / EC_SIM_SEED / EC_CSV_SAMPLE_RATE_HZ` |
| D6 | `configs/README.md` 声明配置带 `created_at`，实际 JSON 无此字段 |
| D7 | `scripts/ingest_public_datasets.py` 的 Zenodo 文件名疑拼写错误（“Conductivtiy_experiment.csv”） |
| D8 | 传感器链路 ID 命名不统一：接口文档 `MOCK_EC_IV` vs soak 脚本 `MOCK_EC_01` |
| D9 | 引用悬空：本文 §12 头部与《项目概述》§11 曾引用 `docs/REVIEW_round4/5` / `REVIEW_round*.md`——这些评审稿仅存在于本地工作副本且**从未被版本跟踪**（clone 后不可见；`docs/` 中入库的仅有 `V2-regression-postmortem.md`）；09-05 更新时已改为转录说明 |

### 12.4 既有已记录问题（docs/已知问题.md）

- P01（200ms 实时性）无精确自动化断言，依赖人工观察。
- P03 30 分钟长跑不进默认套件；Windows + Edge channel 收尾 worker 偶发 300s 强杀（不影响用例结果）。
- 结果区统计仅在 stopped/idle 显示。
- Edge channel 下 `<a download>` 不触发 Playwright download 事件（E2E 已改用 HTTP 断言）。
- 无鉴权、CORS 开发用全放开、PDF/Excel 报表未实现、移动端适配缺失。

---

## 13. 后续路线（roadmap）

| 阶段 | 内容 |
|---|---|
| Phase 4 | DS18B20 温度通道驱动 + 位置验证 + 温度质量标志 |
| Phase 5A/B/C | 电气台架（激励/AFE/ADC/精密负载）→ 导电池 + Kcell 标准液校准 → G/κ(T)/κ25 闭环与温补验证 |
| Phase 6 | pH/ORP 通道（与电导率链路分离） |
| Phase 8 | 判稳/QC 阈值台架标定、PDF 报表（代表值/误差预算/残差） |
| Phase 9 | ESP32-S3 时序控制与同步采集、硬件在环 |
| Phase 10+ | 三电极电化学扩展（恒电位仪/CV），不得倒灌电导率 MVP |

---

## 附录 A：实测验证（Python 3.13.12 / Node 24）

### A.1 2026-09-05 复测（main @ abf7448，Windows 本地）

| 检查项 | 命令 | 结果 |
|---|---|---|
| 后端单测 | `./.venv/Scripts/python -m pytest tests -q` | **131 passed**（较 09-03 +6：PR #18–#20 新增回归测试） |
| 前端单测 | `npm test` | **18/18 passed**（新增 `controlError.test.ts` +3） |
| 前端构建 | `npm run build`（tsc --noEmit + vite build） | 通过 |
| 后端冒烟 | uvicorn :8000 → start → 落库 → stop | 全链路通过（schema v2 帧含 U/I/G/κ/κ25/质量标志，append-only 正常） |

### A.2 2026-09-03 历史记录（原稿基线）

| 检查项 | 命令 | 结果 |
|---|---|---|
| 后端单测 | `./.venv/Scripts/python -m pytest tests -q` | **125 passed**，2 warnings，约 55s（exit 0） |
| 后端 2 条警告 | fastapi.testclient | `StarletteDeprecationWarning: Using httpx with starlette.testclient is deprecated`（第三方库弃用提示，非本项目代码） |
| 后端 2 条警告 | starlette.testclient | `DeprecationWarning: anyio.abc.BlockingPortal alias deprecated`（第三方库弃用提示，非本项目代码） |
| 前端单测 | `npm test` | **15/15 passed**（protocol 解析 15 项，约 0.5s） |
| 前端类型检查 | `npm run typecheck`（tsc --noEmit） | 通过，0 错误 |

> 两条警告均来自第三方库（httpx/starlette/anyio），不阻塞交付。09-05 复测环境未复现警告计数差异，以 A.1 为当前基线。

## 附录 B：常用命令速查

```bash
# 后端单测
cd backend && .venv/Scripts/python -m pytest tests -q
# E2E（Windows 无自带 chromium 时）
cd tests/e2e && npm install && E2E_BROWSER=msedge npx playwright test
# P03 长跑冒烟 / 完整
STABILITY_MINUTES=2 npx playwright test -g "P03"
STABILITY_MINUTES=30 npx playwright test -g "P03"
# 前端
npm run dev | npm run build | npm run typecheck | npm test
```
